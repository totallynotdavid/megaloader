name: lint and format check

on:
  push:
    branches: ["main"]
    paths:
      - "**/*.py"
      - "**/pyproject.toml"
  pull_request:
    branches: ["main"]
    paths:
      - "**/*.py"
      - "**/pyproject.toml"

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@9cfd02964306b527feff5fee75acfd028cce4260 # v7.2.0
        with:
          version: "0.9.10"

      - run: uv python install 3.13.7

      - name: Install workspace dependencies
        run: uv sync --all-packages --extra dev

      - name: Install API dependencies
        run: cd apps/api && uv sync --extra dev

      - uses: astral-sh/ruff-action@4919ec5cf1f49eff0871dbcea0da843445b837e6 # v3.5.1
        with:
          args: "format --check --diff"

      - uses: astral-sh/ruff-action@4919ec5cf1f49eff0871dbcea0da843445b837e6 # v3.5.1
        with:
          args: "check"

      - name: Type check workspace
        run: uv run mypy --exclude apps/api .

      - name: Type check API
        run: cd apps/api && uv run mypy .
