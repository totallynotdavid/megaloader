# Scripts

Utility scripts for the Megaloader project.

## generate-logo.py

Generates the project logo using Python PIL.

```bash
mise run generate-logo
```

Outputs `logo.png` in the root directory.

## Future Scripts

- Version updater: Automated tool to update Python, biome, and other version references across the repo.
- Docs tester: Script to validate code snippets in documentation.